import styles from './BlockContact.module.scss';

export default function BlockContact() {
  return (
    <section className={styles.contact} id={'contact'}>
      <h2>Let's Get in Touch</h2>
      <p>Email me!</p>
    </section>
  );
}
